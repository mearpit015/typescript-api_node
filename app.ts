let hello = "Hi arpit";
console.log(hello);

/* basic program for ts  */
let checkFor = (num: number, num2: number) =>{

    for (let index = 0; index <=num; index++) {
        console.log((num +index) * num2);
    }
}

checkFor(5, 20);